package hennovo_backend.plantillacarga.repositorys;

import org.springframework.data.jpa.repository.JpaRepository;

import hennovo_backend.plantillacarga.entitys.CeldaPlantilla;

public interface CeldaPlantillaRepository extends JpaRepository<CeldaPlantilla, Long> {
}
